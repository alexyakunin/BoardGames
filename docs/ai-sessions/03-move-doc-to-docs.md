# Session 03

**User:** There are `doc` and `docs` folders in this repo — move the content from `doc` into `docs`.

**Opus4.8:** Moved `doc/img/BoardGames.jpg` to `docs/img/BoardGames.jpg` with `git mv`, updated the image reference in `README.md`, and removed the now-empty `doc/` folder.

**User:** Commit and push.

**Opus4.8:** Committed the move and pushed to `main`.
